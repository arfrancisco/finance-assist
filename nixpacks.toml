[phases.setup]
nixPkgs = ["ruby_3_4", "postgresql_16", "libyaml", "pkg-config"]

[phases.install]
cmds = ["bundle install"]

[phases.build]
cmds = ["bundle exec rails assets:precompile 2>/dev/null || true"]

[start]
cmd = "bundle exec rails server -b 0.0.0.0 -p $PORT"
